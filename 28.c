//Binary SEARCH to find key element in MID of array
#include<stdio.h>
int main(){
    int n, top, bottom, i, j, mid, key;
    int A[6]= {10, 12, 14, 15, 17, 22};

    printf("Enter key element:");
    scanf("%d, &key");
    

}